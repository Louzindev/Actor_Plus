{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "config",
            "type": "shell",
            "command": "${workspaceFolder}\\Actor_Plus\\make config-project"
        },
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}\\Actor_Plus\\make build-project"
        },
    ]
}